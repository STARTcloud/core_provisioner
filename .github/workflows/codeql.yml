name: 'CodeQL Advanced'

# Reusable CodeQL scan, chained from ci.yml and run weekly on a schedule.
# Mirrors the startcloud_generic_provisioner / hyperweaver-ui layout. Ruby is
# scanned HERE — consumer repos ship this code as their driver and skip it in
# their own CodeQL configs.

on:
  schedule:
    - cron: '17 5 * * 4'
  workflow_call:

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: ruby
            build-mode: none

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: '/language:${{matrix.language}}'
