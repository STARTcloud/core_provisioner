## Don't save secrets
.secrets.yml
root-ca.key

## Don't save log files
*.log

# Don't include Vagrant support files
.vagrant
Hosts.yml
results.yml
support-bundle.zip
disks/
*.vdi

# Driver bootstrap artifacts (dev-local testing of the stub in this repo;
# consumers commit their own driver.version — this ignore is core-repo-only)
driver/
driver.version

# Tooling
.claude

# Editor / OS noise
.DS_Store
*.swp
.vscode/
.settings/
